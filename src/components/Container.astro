---
const { class: className, id } = Astro.props
---

<section
  id={`${Astro.props.id}`}
  class={`w-full mx-auto max-w-[1000px] mb-5 overflow-hidden px-8 ${Astro.props.class} `}
>
  <slot />
</section>
